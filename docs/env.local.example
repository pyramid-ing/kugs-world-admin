### Supabase (필수)
# Supabase Dashboard → (프로젝트) → Settings → API 에서 확인
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"

# 아래 값은 "Project API keys"에서 가져옵니다.
# 권장: Publishable key (sb_publishable_...).
# 호환용: Legacy anon key (JWT 형태 eyJ...).
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_XXXXXXXXXXXXXXXXXXXXXXXX"

### Supabase MCP/CLI 자동화 (선택)
# 제가 MCP로 RLS 적용/테스트를 하려면 필요합니다.
# Supabase 계정 → Access Tokens(또는 Personal Access Tokens)에서 발급
# SUPABASE_ACCESS_TOKEN="sbp_XXXXXXXXXXXXXXXXXXXXXXXX"


